# macOS
.DS_Store
**/.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# Python
__pycache__/
*.pyc
*.pyo

# Node (defensive)
/node_modules/

# Claude Code local state (do NOT push session files)
/.claude/

# Local-only data — large media & reference (not deployed)
# These directories stay on your Mac and are NOT part of the Vercel site.
/book/
/gumroad video/
/video/
# Gumroad source assets stay local, but the 26 social-share covers referenced
# by live Open Graph / Twitter metadata must ship with the website.
/gumroad-covers/*
!/gumroad-covers/*_Australia_cover.jpg
!/gumroad-covers/OpenWays_Edge_cover.jpg
/products/
/production/
/reference/

# Local environment
.env
.env.local

# Backup files (备份，不入仓)
*.OLD.html
*.OLD
*.OLD.md
